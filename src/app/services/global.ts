export var GLOBAL = { //esta variable se exporta en las clases service, lo hace para ahorrar código
  url: ' http://localhost:3977 ', //url de nuestra api REST con nodejs
  //ip: '127.0.0.1' // esto no lo vamos a usar, pero se podrian colocar esta o mas variables aqui en GLOBAL
};
